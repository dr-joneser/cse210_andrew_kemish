class GenerateNewObj {
    
    //Randomly generate new rock and gem objects and add to list that contains all the objects
    //Need to be in a single list so that we can run through a for loop and check for collisions
    static Random rnd = new Random();
    int randomPos  = rnd.Next(1, 801);
}